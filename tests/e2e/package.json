{
  "name": "e2e-tests",
  "private": true,
  "version": "1.0.0",
  "description": "True end-to-end tests: a real browser against the real frontend + backend + Supabase dev project. Self-contained on purpose - see playwright.config.js - so this never touches backend/ or frontend/ package.json.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "dotenv": "^17.4.2"
  }
}
